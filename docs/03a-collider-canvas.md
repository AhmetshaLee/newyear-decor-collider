# Документ 3a: Холст Коллайдера

Этот документ описывает поведение экрана `/collider`: изолированный холст, фиксированный фрейм прибора, панорамирование и управление масштабом. Бизнес-логика крафта, элементы панели, расчет цены и инварианты прибора описаны в `docs/03-collider.md`.

---

## 1. Концептуальная модель

Экран Коллайдера работает по модели рабочей области графического редактора:

- **Холст:** видимая область страницы, занимающая весь экран пользователя.
- **Фрейм (Frame):** сам прибор Коллайдер с фиксированным базовым размером `1280x720 px`.
- **Слой управления:** слой поверх холста, на котором размещаются элементы управления отображением. Он не является частью прибора и не масштабируется вместе с фреймом.

Браузерное выделение текста отключено только внутри фрейма прибора. Слой управления и будущие панели поверх холста сохраняют стандартное выделение текста.

Масштабирование применяется только к фрейму прибора. Внутренняя координатная сетка и пропорции элементов внутри прибора остаются статичными относительно границ фрейма.

---

## 2. Разделение типов страниц

Страницы приложения используют два разных режима верстки:

1. **Стандартная адаптивная страница** (`/calendar`):
   - адаптируются под размеры экрана через обычные механики верстки;
   - при нехватке вертикального места используют стандартный системный скроллинг браузера.
2. **Экран Коллайдера** (`/collider`):
   - занимает весь экран пользователя;
   - блокирует стандартный скроллинг браузера;
   - управляет отображением прибора собственными механизмами холста.

Панели Инвентаря и Книги коллекций входят в маршрут Коллайдера через query-параметр `panel`. Они открываются в верхнем слое браузера поверх холста и не масштабируются вместе с фреймом прибора.

---

## 3. Фрейм прибора

Фрейм прибора имеет жесткий базовый размер:

- ширина: `1280px`;
- высота: `720px`.

Фрейм позиционируется в центре холста. Все элементы внутри прибора проектируются относительно базового размера `1280x720 px`.

Адаптивность экрана достигается масштабированием всего фрейма, а не перестройкой его внутренних блоков. При изменении размера окна браузера прибор сохраняет пропорции и пересчитывает масштаб относительно доступной области.

---

## 4. Автоматическое вписывание

При открытии экрана Коллайдера рассчитывается базовый коэффициент вписывания фрейма в текущую видимую область.

Фрейм должен помещаться в экран с небольшим визуальным отступом от границ. Базовый масштаб выбирается по меньшей стороне доступной области, чтобы прибор не обрезался по ширине или высоте.

Первичное вписывание выполняется до первого отображаемого кадра. При последующем непрерывном изменении размеров холста перерасчет выполняется через `100ms` после последнего изменения, чтобы тяжелый фрейм не масштабировался на каждый промежуточный пиксель.

---

## 5. Пользовательское масштабирование

После автоматического вписывания пользователь может вручную увеличить или уменьшить итоговый масштаб прибора.

Пользовательское масштабирование:

- стартует с фактического коэффициента автоматического вписывания;
- изменяется кнопками с множителем `1.2` и плавно колесом мыши;
- ограничивается единым итоговым диапазоном от `50%` до `125%`;
- имеет нижнюю и верхнюю границы;
- хранится только во временном UI-состоянии;
- не записывается в `localStorage`.

Холст хранит один текущий итоговый коэффициент. Автоматический коэффициент вписывания используется как исходное значение, точка сброса и основа для сохранения относительного приближения при изменении размеров.

Колесо мыши изменяет масштаб относительно позиции курсора без CSS-перехода (`transition`): точка фрейма под курсором сохраняет экранную позицию. Серия событий колеса мыши (`wheel`) объединяется так, чтобы преобразование (`transform`) применялось не чаще одного раза за кадр. Кнопки увеличивают текущий коэффициент в `1.2` раза или уменьшают делением на `1.2`; поэтому промежуточные проценты не обязаны быть круглыми.

На время серии wheel-событий фрейм временно переводится в отдельный композитный слой. После завершения ввода слой освобождается, чтобы не ухудшать четкость текста в состоянии покоя.

---

## 6. Панель масштаба

Управление масштабом расположено на слое управления, вне фрейма прибора. Панель остается видимой и не масштабируется вместе с прибором.

Слой управления является отдельной интерактивной областью. Нажатие на его элементы, фон или внутренние отступы не должно передаваться холсту и запускать панорамирование.

Курсор определяется владельцем области: холст использует `grab` или `grabbing`, подложка панели масштаба — `default`, активные кнопки и ссылки — `pointer`. Панель управления не должна наследовать курсор холста.

Панель содержит:

- `[-]` — уменьшить пользовательский масштаб;
- `[R]` — сбросить пользовательский масштаб к базовому значению и вернуть фрейм в центр;
- `[+]` — увеличить пользовательский масштаб.

Панель показывает единственный фактический итоговый масштаб прибора в процентах.

Кнопки `[-]` и `[+]` блокируются на итоговых границах `50%` и `125%`.

---

## 7. Панорамирование

Холст и неинтерактивные участки корпуса поддерживают перемещение фрейма удержанием основной кнопки указателя. Кнопки, ссылки, поля ввода и диалоги сохраняют обычное интерактивное поведение и не запускают панорамирование.

Перемещение ограничено по обеим осям так, чтобы часть фрейма всегда оставалась доступной на холсте. Минимальная видимая часть по каждой оси равна четверти меньшего размера между фреймом и холстом.

Смещение хранится только до перезагрузки страницы. Инерционное перемещение не используется.

---

## 8. Контракт компоновки (`layout`) корпуса

Внутренняя компоновка фрейма должна оставаться привязанной к фиксированному размеру `1280x720 px`. Базовая скелетная декомпозиция корпуса:

- статическая поверхность корпуса изолирована от расположенного поверх нее DOM-слоя элементов управления;
- левая зона: блоки настроек прибора;
- центральная зона: информационный дисплей и кнопки открытия Инвентаря и Книги коллекций;
- правая зона: блок выбора типа украшения и запуск крафта.

Детальные правила монитора, роторов, тумблеров, выбора типа украшения, антиповторителя, кнопки запуска и расчета стоимости находятся в `docs/03-collider.md`.
